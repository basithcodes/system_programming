#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>
#include <stdlib.h>

int main(void) {
	int pipefds[2];
	char *buffer[5];
	char *str = "viven";
	exit(EXIT_FAILURE);

	if (pipe(pipefds) == -1) {

	}
}
